require('dotenv').config();
const express = require('express');
const cors    = require('cors');
const multer  = require('multer');
const axios   = require('axios');

const app    = express();
const upload = multer({ storage: multer.memoryStorage(), limits: { fileSize: 10 * 1024 * 1024 } });

app.use(cors({ origin: '*' }));
app.use(express.json({ limit: '20mb' }));
app.use(express.urlencoded({ extended: true, limit: '20mb' }));

const GEMINI_API_KEY = process.env.GEMINI_API_KEY;
const GEMINI_URL     = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent?key=${GEMINI_API_KEY}`;

// ─── Промпты ─────────────────────────────────────────────────────────────────
const PROMPTS = {

  plant: `Ты эксперт-ботаник. Посмотри на фото. Если на фото НЕ растение — верни {"wrong_category": "На фото не растение. Выбери подходящую категорию на главном экране."}.
Если растение — ответь строго в формате JSON:
{"name":"Название на русском","latin":"Латинское название","type":"дерево"|"кустарник"|"трава"|"цветок"|"овощ"|"фрукт"|"ягода"|"другое","safe":true|false,"confidence":"высокая"|"средняя"|"низкая","description":"2-3 предложения","warning":"предупреждение или null","signs":["признак 1","признак 2","признак 3"]}
Отвечай ТОЛЬКО JSON.`,

  mushroom: `Ты эксперт-миколог. Посмотри на фото. Если на фото НЕ гриб — верни {"wrong_category": "На фото не гриб. Выбери подходящую категорию на главном экране."}.
Если гриб — ответь строго в формате JSON:
{"name":"Название на русском","latin":"Латинское название","edible":"съедобный"|"условно съедобный"|"несъедобный"|"ядовитый"|"смертельно ядовитый","confidence":"высокая"|"средняя"|"низкая","description":"2-3 предложения","warning":"предупреждение если опасен или null","signs":["признак 1","признак 2","признак 3"]}
Отвечай ТОЛЬКО JSON.`,

  tree: `Ты эксперт-дендролог. Посмотри на фото. Если на фото НЕ дерево или кустарник — верни {"wrong_category": "На фото не дерево. Выбери подходящую категорию на главном экране."}.
Если дерево — ответь строго в формате JSON:
{"name":"Название на русском","latin":"Латинское название","type":"дерево"|"кустарник"|"хвойное"|"лиственное"|"плодовое","safe":true|false,"confidence":"высокая"|"средняя"|"низкая","description":"2-3 предложения: где растёт, особенности, применение","warning":"если ядовито или опасно, иначе null","signs":["признак 1","признак 2","признак 3"]}
Отвечай ТОЛЬКО JSON.`,

  berry: `Ты эксперт-ботаник. Посмотри на фото. Если на фото НЕ ягода — верни {"wrong_category": "На фото не ягода. Выбери подходящую категорию на главном экране."}.
Если ягода — ответь строго в формате JSON:
{"name":"Название на русском","latin":"Латинское название","edible":"съедобная"|"условно съедобная"|"несъедобная"|"ядовитая"|"смертельно ядовитая","confidence":"высокая"|"средняя"|"низкая","description":"2-3 предложения: где растёт, вкус, применение","warning":"если ядовита или опасна, иначе null","signs":["признак 1","признак 2","признак 3"]}
Отвечай ТОЛЬКО JSON.`,

  weed: `Ты эксперт-агроном. Посмотри на фото. Если на фото НЕ сорняк или сорное растение — верни {"wrong_category": "На фото не сорняк. Выбери подходящую категорию на главном экране."}.
Если сорняк — ответь строго в формате JSON:
{"name":"Название на русском","latin":"Латинское название","danger":"низкая"|"средняя"|"высокая","confidence":"высокая"|"средняя"|"низкая","description":"2-3 предложения: почему вреден","removal":"Как избавиться — 1-2 предложения","signs":["признак 1","признак 2","признак 3"]}
Отвечай ТОЛЬКО JSON.`,

  disease: `Ты эксперт-фитопатолог. Посмотри на фото. Если на фото НЕ больное растение или признаки болезни — верни {"wrong_category": "На фото не видна болезнь растения. Выбери подходящую категорию."}.
Если болезнь — ответь строго в формате JSON:
{"name":"Название болезни или вредителя на русском","type":"болезнь"|"вредитель"|"дефицит питания"|"другое","severity":"лёгкая"|"средняя"|"тяжёлая","confidence":"высокая"|"средняя"|"низкая","description":"2-3 предложения: симптомы и причины","treatment":"Как лечить — 1-2 предложения","signs":["симптом 1","симптом 2","симптом 3"]}
Отвечай ТОЛЬКО JSON.`,

  seed: `Ты эксперт-ботаник. Посмотри на фото. Если на фото НЕ семена или плоды — верни {"wrong_category": "На фото не семена. Выбери подходящую категорию на главном экране."}.
Если семена — ответь строго в формате JSON:
{"name":"Название растения на русском","latin":"Латинское название","type":"овощ"|"цветок"|"трава"|"дерево"|"другое","confidence":"высокая"|"средняя"|"низкая","description":"2-3 предложения: когда сеять, как прорастает, особенности","signs":["признак 1","признак 2","признак 3"]}
Отвечай ТОЛЬКО JSON.`,

  vegetable: `Ты эксперт-кулинар и агроном. Посмотри на фото. Если на фото НЕ овощ — верни {"wrong_category": "На фото не овощ. Выбери подходящую категорию на главном экране."}.
Если овощ — ответь строго в формате JSON:
{"name":"Название на русском","latin":"Латинское название","confidence":"высокая"|"средняя"|"низкая","description":"2-3 предложения: происхождение, особенности","usage":"Как использовать в кулинарии — 1-2 предложения","signs":["признак 1","признак 2","признак 3"]}
Отвечай ТОЛЬКО JSON.`,

  fruit: `Ты эксперт-кулинар и ботаник. Посмотри на фото. Если на фото НЕ фрукт — верни {"wrong_category": "На фото не фрукт. Выбери подходящую категорию на главном экране."}.
Если фрукт — ответь строго в формате JSON:
{"name":"Название на русском","latin":"Латинское название","confidence":"высокая"|"средняя"|"низкая","description":"2-3 предложения: откуда, особенности, польза","usage":"Как использовать — 1-2 предложения","signs":["признак 1","признак 2","признак 3"]}
Отвечай ТОЛЬКО JSON.`,

  bird: `Ты эксперт-орнитолог. Посмотри на фото. Если на фото НЕ птица — верни {"wrong_category": "На фото не птица. Выбери подходящую категорию на главном экране."}.
Если птица — ответь строго в формате JSON:
{"name":"Название на русском","latin":"Латинское название","confidence":"высокая"|"средняя"|"низкая","description":"2-3 предложения: где обитает, чем питается, особенности","signs":["признак 1","признак 2","признак 3"]}
Отвечай ТОЛЬКО JSON.`,

  insect: `Ты эксперт-энтомолог. Посмотри на фото. Если на фото НЕ насекомое или членистоногое — верни {"wrong_category": "На фото не насекомое. Выбери подходящую категорию на главном экране."}.
Если насекомое — ответь строго в формате JSON:
{"name":"Название на русском","latin":"Латинское название","dangerous":true|false,"confidence":"высокая"|"средняя"|"низкая","description":"2-3 предложения: где живёт, чем питается, польза или вред","warning":"если опасно для человека или сада, иначе null","signs":["признак 1","признак 2","признак 3"]}
Отвечай ТОЛЬКО JSON.`,

  animal: `Ты эксперт-зоолог. Посмотри на фото. Если на фото НЕ животное — верни {"wrong_category": "На фото не животное. Выбери подходящую категорию на главном экране."}.
Если животное — ответь строго в формате JSON:
{"name":"Название на русском","latin":"Латинское название","confidence":"высокая"|"средняя"|"низкая","description":"2-3 предложения: ареал, образ жизни, особенности","warning":"если опасно для человека, иначе null","signs":["признак 1","признак 2","признак 3"]}
Отвечай ТОЛЬКО JSON.`,

  track: `Ты эксперт-следопыт и зоолог. Посмотри на фото. Если на фото НЕ следы животного — верни {"wrong_category": "На фото не следы животного. Выбери подходящую категорию на главном экране."}.
Если следы — ответь строго в формате JSON:
{"name":"Название животного на русском","latin":"Латинское название","confidence":"высокая"|"средняя"|"низкая","description":"2-3 предложения: как выглядят следы, поведение животного","warning":"если животное опасно, иначе null","signs":["признак следа 1","признак следа 2","признак следа 3"]}
Отвечай ТОЛЬКО JSON.`,

  rock: `Ты эксперт-геолог и минералог. Посмотри на фото. Если на фото НЕ камень, минерал или горная порода — верни {"wrong_category": "На фото не камень или минерал. Выбери подходящую категорию на главном экране."}.
Если камень — ответь строго в формате JSON:
{"name":"Название на русском","latin":"Латинское или научное название","confidence":"высокая"|"средняя"|"низкая","description":"2-3 предложения: происхождение, свойства, где встречается","signs":["признак 1","признак 2","признак 3"]}
Отвечай ТОЛЬКО JSON.`,

  mystery: `Ты универсальный эксперт. Посмотри на фото и определи что на нём изображено. Ответь строго в формате JSON:
{"name":"Что это — название на русском","category":"к какой категории относится (растение/гриб/животное/птица/насекомое/камень/овощ/фрукт/другое)","confidence":"высокая"|"средняя"|"низкая","description":"2-3 предложения: что это такое и основные факты","suggestion":"Если это входит в одну из наших категорий (растение, гриб, ягода, дерево, сорняк, болезнь растения, семена, овощ, фрукт, птица, насекомое, животное, следы, камень) — предложи пользователю выбрать эту категорию для получения подробной информации. Иначе null.","signs":["факт 1","факт 2","факт 3"]}
Отвечай ТОЛЬКО JSON.`

};

function extractJSON(text) {
  const clean = text.trim().replace(/^```json\s*/i, '').replace(/```\s*$/, '').trim();
  return JSON.parse(clean);
}

app.get('/', (req, res) => {
  res.json({ status: 'ok', service: 'nature-scanner-backend', ai: 'gemini-2.5-flash' });
});

app.post('/scan', upload.single('photo'), async (req, res) => {
  try {
    let imageBase64, mediaType;
    if (req.file) {
      imageBase64 = req.file.buffer.toString('base64');
      mediaType   = req.file.mimetype || 'image/jpeg';
    } else if (req.body.base64) {
      const raw   = req.body.base64;
      const match = raw.match(/^data:(image\/\w+);base64,(.+)$/);
      if (match) { mediaType = match[1]; imageBase64 = match[2]; }
      else { imageBase64 = raw; mediaType = 'image/jpeg'; }
    } else {
      return res.status(400).json({ error: 'Нет изображения' });
    }

    const mode   = req.body.mode || 'plant';
    const prompt = PROMPTS[mode];
    if (!prompt) return res.status(400).json({ error: 'Неизвестный режим: ' + mode });

    const response = await axios.post(
      GEMINI_URL,
      {
        contents: [{ parts: [
          { inline_data: { mime_type: mediaType, data: imageBase64 } },
          { text: prompt }
        ]}],
        generationConfig: { temperature: 0.1, maxOutputTokens: 1000 }
      },
      { headers: { 'Content-Type': 'application/json' }, timeout: 30000 }
    );

    const rawText = response.data?.candidates?.[0]?.content?.parts?.[0]?.text || '';

    let result;
    try {
      result = extractJSON(rawText);
    } catch (e) {
      console.error('JSON parse error:', rawText);
      return res.status(422).json({ error: 'Не удалось обработать ответ. Попробуй ещё раз.' });
    }

    if (result.error) return res.status(422).json({ error: result.error });

    res.json({ mode, result });

  } catch (err) {
    console.error('scan error:', err.response?.data || err.message);
    res.status(500).json({ error: 'Ошибка сервера. Попробуй позже.' });
  }
});

const PORT = process.env.PORT || 3000;
app.listen(PORT, () => console.log(`Nature Scanner running on port ${PORT}`));
